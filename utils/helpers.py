# def get_summary_token_limits(length: str):
#     """
#     Returns (min_length, max_length) tuple for the summary based on user choice.
#     """
#     options = {
#         "short": (10, 30),
#         "medium": (30, 60),
#         "long": (60, 120)
#     }
#     return options.get(length.lower(), (30, 60))
